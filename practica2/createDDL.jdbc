CREATE TABLE PERSONA (ID VARCHAR(255) NOT NULL, APELLIDOS VARCHAR(255), NOMBRE VARCHAR(255), FAMILIA_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE FAMILIA (ID INTEGER NOT NULL, DESCRIPCION VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE EMPLEO (ID INTEGER NOT NULL, DESCRIPCIONTRABAJO VARCHAR(255), SALARIO FLOAT, PRIMARY KEY (ID))
CREATE TABLE PERSONA_EMPLEO (Persona_ID VARCHAR(255) NOT NULL, listaEmpleos_ID INTEGER NOT NULL, PRIMARY KEY (Persona_ID, listaEmpleos_ID))
ALTER TABLE PERSONA ADD CONSTRAINT PERSONA_FAMILIA_ID FOREIGN KEY (FAMILIA_ID) REFERENCES FAMILIA (ID)
ALTER TABLE PERSONA_EMPLEO ADD CONSTRAINT PRSNMPLEOlstmplsID FOREIGN KEY (listaEmpleos_ID) REFERENCES EMPLEO (ID)
ALTER TABLE PERSONA_EMPLEO ADD CONSTRAINT PRSNAEMPLEOPrsnaID FOREIGN KEY (Persona_ID) REFERENCES PERSONA (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN_TABLE', 0)
